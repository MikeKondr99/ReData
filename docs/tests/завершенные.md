# Завершенные баги

## BUG-007
- Кратко: `orderByName` для отсутствующего поля теперь считается допустимым сценарием (не сортировать, без ошибки).
- Статус: закрыт как ожидаемая логика (не баг).

## BUG-008
- Кратко: `PUT /api/datasets/{id}` больше не требует `id` в body; обновление проходит с `id` только из path.
- Статус: исправлен.

## BUG-010
- Кратко: дефолт `select.restOptions` без явного значения трактуется как `Delete`.
- Статус: не баг, легаси-решение (зафиксировано как ожидаемое поведение).
